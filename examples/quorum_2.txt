use LED
use BUTTON
class MyClass
        LED led
        BUTTON btn
        action Main()
                led:pinLED(2)
                btn:pinBTN(3)
                integer a
                a = btn:STATE()
                if a = 1
                        led:ON()
                else
                        led:OFF()
                end
        end
end
